
// This is a utility script to ensure the build:dev script exists
// It's a workaround since we can't directly modify package.json
console.log("Setting up build environment...");
// The actual build will be handled by the normal build process
// This script is just a placeholder to work around the package.json issue
